name: Telegram Bot Listener
on:
  schedule:
    - cron: '0 3 * * 1-5'    # 8:30 AM IST
    - cron: '0 7 * * 1-5'    # 12:30 PM IST
    - cron: '30 10 * * 1-5'  # 4:00 PM IST
    - cron: '0 13 * * 1-5'   # 6:30 PM IST
    - cron: '30 14 * * 1-5'  # 8:00 PM IST — after evening report
  workflow_dispatch:
jobs:
  bot:
    runs-on: ubuntu-latest
    timeout-minutes: 58
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    - run: pip install yfinance ta pandas requests gspread google-auth pytz
    - run: python telegram_bot.py
      env:
        TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
        TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        GOOGLE_SHEET_ID: ${{ secrets.GOOGLE_SHEET_ID }}
        GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
